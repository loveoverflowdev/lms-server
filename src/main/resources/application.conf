ktor {
    deployment {
        port = "8080"
        port = ${?PORT}

        host = "127.0.0.1"
        host = ${?HOST}
    }
}

jwt {
    host = "127.0.0.1"
    host = ${?HOST}

    secret = "unstoppable"
    issuer = "http://${host}:8080/"
    audience = "http://${host}:8080/hello"
    realm = "CSKH"
}
