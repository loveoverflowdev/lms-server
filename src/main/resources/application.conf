ktor {
    deployment {
        port = "8080"
        port = ${?PORT}

        host = "0.0.0.0"
        host = ${?HOST}
    }
}

jwt {
    host = "0.0.0.0"
    host = ${?HOST}

    secret = "unstoppable"
    issuer = "http://${host}:8080/"
    audience = "http://${host}:8080/hello"
    realm = "CSKH"
}
